export default function ComingSoon({ title }) {
  return (
    <section className="coming-soon">
      <p className="eyebrow eyebrow--dark">Coming Soon</p>
      <h2>{title}</h2>
      <p>This section is being put together — check back soon for updates.</p>
    </section>
  )
}
